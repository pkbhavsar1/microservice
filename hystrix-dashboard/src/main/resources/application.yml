#HYSTRIX DASHBOARD
server:
  port: 9295
  
spring:
  application:
    name: HYSTRIX-DASHBOARD
  config:
    import: "optional:configserver:"
    
hystrix:
  dashboard:
    proxy-stream-allow-list: "*"  